import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  languageWrapper: {
    gap: 20,
    justifyContent: "space-around",
  },
  languageTitle: {
    fontSize: 20,
    alignSelf: "center",
  },
});

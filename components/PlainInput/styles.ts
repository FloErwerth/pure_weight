import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    innerWrapper: {
        alignSelf: "stretch",
        padding: 5,
    },
    inputStyle: {
        borderWidth: 0,
        flex: 1,
        marginRight: 10,
        backgroundColor: "transparent",
    },
});

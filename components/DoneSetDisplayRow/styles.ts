import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  innerWrapper: {
    alignItems: "center",
    alignSelf: "stretch",
    justifyContent: "space-around",
  },
});

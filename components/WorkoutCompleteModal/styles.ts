import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    margin: { marginBottom: 10 },
    gap: { gap: 5 },
    wrapper: { alignSelf: "stretch", padding: 5, paddingBottom: 8 },
    innerWrapper: { marginBottom: 5, justifyContent: "space-between" },
    smallGap: { gap: 3 },
    text: { fontSize: 26 },
    unitText: { fontSize: 20, alignSelf: "flex-end", marginBottom: 2 },
    smallText: { fontSize: 12 },
});

import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  stack: {
    gap: 20,
    marginBottom: 10,
  },
  set: {
    alignSelf: "center",
  },
  buttonIcon: {
    pointerEvents: "none",
    position: "relative",
    left: -3,
    top: 3,
    height: 30,
  },
  confirmButton: {
    width: 20,
    alignItems: "center",
    justifyContent: "center",
  },
  box: {
    width: 40,
  },
});
